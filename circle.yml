
machine:
  node:
    version: 6
  post:
    - npm install -g npm@^4

test:
  override:
    - make verify
    - make test

deployment:
  staging:
    branch: master
    commands:
      - make deploy-ci
